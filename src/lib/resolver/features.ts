import type { GrantBundle } from '@/types/sources'
import type { ResolvedFeature } from '@/types/resolved'

export function resolveFeatures(_bundles: readonly GrantBundle[]): readonly ResolvedFeature[] {
  return []
}
